<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view"
    >
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/menuFragment"
            android:icon="@drawable/ic_menu_weather"
            android:title="@string/Home" />
        <item
            android:id="@+id/weatherListFragment"
            android:icon="@drawable/ic_list_weather"
            android:title="@string/weather_List" />
    </group>

    <item android:title="@string/group_Manage">
        <menu>
            <item
                android:id="@+id/weatherFragment"
                android:icon="@drawable/ic_add_weather"
                android:title="@string/add_weather" />

            <item
                android:id="@+id/mapsFragment"
                android:icon="@drawable/maps_location_icon"
                android:title="@string/add_weather_via_map" />
        </menu>
    </item>

    <item android:title="@string/theme_control">
        <menu>
            <item
                android:id="@+id/weatherTheme"
                android:icon="@drawable/theme_icon"
                android:title="@string/change_theme" />

        </menu>
    </item>

    <item android:title="@string/account_control">
        <menu>
            <item
                android:id="@+id/nav_sign_out"
                android:icon="@android:drawable/ic_menu_revert"
                android:title="@string/sign_out" />
        </menu>
    </item>
</menu>